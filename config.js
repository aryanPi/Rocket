const config = {
  databaseHost: "localhost",
  databasePort: 3306,
  databaseUser: "root",
  databasePassword: "password",
  databaseDatabase: "crash",

  serverPort: 80,
};

module.exports.config = config;
